# Core
pandas==2.3.3
numpy==2.2.6
python-dotenv==1.1.0
nest_asyncio==1.6.0
requests>=2.31.0

# Technical Analysis
pandas_ta==0.4.71b0

# Binance API
python-binance==1.0.36
websockets==16.0

# Notifications
python-telegram-bot==22.7
APScheduler==3.11.2
telethon>=1.37.0

# System monitoring
psutil==7.0.0

# Machine Learning
# Pinned to the version the regime classifiers are trained with (conda 1.6.1)
# so the loaded .pkl matches the runtime — avoids the InconsistentVersion warning
# and guarantees prediction consistency (signal image + CI test env + training).
scikit-learn==1.6.1

# Backtesting
vectorbt==1.0.0

# Testing
pytest==8.3.4
pytest-asyncio>=0.23
pyyaml==6.0.2
