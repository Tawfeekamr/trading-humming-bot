[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "trading_engine_core"
version = "0.2.0"
description = "Rust trading engine core with PyO3 indicator bindings"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
