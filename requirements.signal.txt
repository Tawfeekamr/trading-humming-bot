# Signal-listener + regime-pusher deps — a subset of requirements.txt.
# The listener imports: yaml (pyyaml), dotenv (python-dotenv), psutil,
# pandas + pandas_ta (Phase-2 feature compute via src.data.feature_engineering),
# and telethon (channel listener). The regime-pusher sidecar (same image,
# `python -m src.ml.regime_pusher`) additionally needs scikit-learn (load/predict
# the RF regime .pkl) and requests (call the Rust engine HTTP API).
# Everything else in requirements.txt (python-binance, websockets, aiohttp,
# apscheduler, python-telegram-bot, ...) is unused by this image.
pyyaml>=6.0
python-dotenv==1.1.0
psutil==7.0.0
pandas==2.3.3
pandas_ta==0.4.71b0
telethon>=1.37.0
scikit-learn>=1.5.0
requests>=2.31
