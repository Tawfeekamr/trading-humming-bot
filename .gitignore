# Environment
.env
.env.docker
.claude/
keys/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Data & Logs
/data/
trading-engine-core/data/
backtest/data_cache/
backtest/results/
models/*.pkl
# Exception: the reproducible regime classifiers the live regime-pusher ships.
# Small (~0.5 MB each), versioned so EC2's deploy-time `git pull` delivers them
# alongside the code (no SCP/S3 transfer needed; SSM-only host, no SSH key).
!models/regime_*_clean.pkl
models/rl/*.log
# walk-forward per-slice research checkpoints (regenerable via the sweep)
models/rl/_wf_*
logs/
reports/
tb/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Terraform state
iac/**/*.tfstate*
iac/**/.terraform/

# Rust build artifacts
trading-engine-core/target/

# local pre-retrain safety backup (not for commit)
models/_pre_retrain_backup_*/
