name: Weekly Parameter Sweep

on:
  schedule:
    - cron: '0 0 * * 0'  # Every Sunday at 00:00 UTC
  workflow_dispatch:  # Allow manual trigger

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: backtest-sweep
  AWS_REGION: "ap-northeast-1"

jobs:
  # ── Job 1: Build & push sweep image to GHCR ───────────────────
  build:
    name: Build Sweep Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,format=short
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: backtest/Dockerfile.sweep
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Job 2: Run sweep on EC2 via SSM ───────────────────────────
  run-sweep:
    name: Run Sweep on EC2
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Write sweep script to EC2
        run: |
          SCRIPT_COMMAND_ID=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --instance-ids "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --parameters commands='[
              "cat > /tmp/sweep.sh << '\''SCRIPT'\''",
              "#!/bin/bash",
              "set -e",
              "cd /home/ec2-user/trading-humming-bot",
              "git pull origin main 2>&1",
              "docker pull ghcr.io/tawfeekamr/backtest-sweep:latest 2>&1",
              "mkdir -p backtest/results backtest/data_cache",
              "for PAIR in ETHUSDT BNBUSDT DOGEUSDT XRPUSDT; do",
              "  echo \"=== Sweeping ${PAIR} ===\"",
              "  docker run --rm -v /home/ec2-user/trading-humming-bot/backtest/results:/sweep/backtest/results -v /home/ec2-user/trading-humming-bot/backtest/data_cache:/sweep/backtest/data_cache ghcr.io/tawfeekamr/backtest-sweep:latest --pair ${PAIR} --output backtest/results/${PAIR}_sweep.json 2>&1",
              "  echo \"=== ${PAIR} done ===\"",
              "done",
              "echo SWEEP_COMPLETE",
              "SCRIPT",
              "chmod +x /tmp/sweep.sh"
            ]' \
            --timeout-seconds 30 \
            --comment "Write sweep script" \
            --query "Command.CommandId" \
            --output text)

          for i in $(seq 1 6); do
            STATUS=$(aws ssm get-command-invocation --command-id "$SCRIPT_COMMAND_ID" \
              --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
              --query "Status" --output text 2>/dev/null || echo "Pending")
            [ "$STATUS" = "Success" ] && break
            [ "$STATUS" = "Failed" ] && exit 1
            sleep 5
          done

      - name: Run sweep on EC2
        id: sweep
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --instance-ids "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --parameters commands='["bash /tmp/sweep.sh"]' \
            --timeout-seconds 900 \
            --comment "Weekly parameter sweep" \
            --query "Command.CommandId" \
            --output text)

          echo "Sweep command: $COMMAND_ID"
          for i in $(seq 1 60); do
            STATUS=$(aws ssm get-command-invocation \
              --command-id "$COMMAND_ID" \
              --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
              --query "Status" --output text 2>/dev/null || echo "Pending")
            echo "Attempt $i: $STATUS"
            if [ "$STATUS" = "Success" ]; then
              echo "=== SWEEP OUTPUT ==="
              aws ssm get-command-invocation --command-id "$COMMAND_ID" \
                --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
                --query "StandardOutputContent" --output text
              exit 0
            elif [ "$STATUS" = "Failed" ]; then
              echo "=== SWEEP FAILED ==="
              aws ssm get-command-invocation --command-id "$COMMAND_ID" \
                --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
                --query "StandardErrorContent" --output text
              echo "--- stdout ---"
              aws ssm get-command-invocation --command-id "$COMMAND_ID" \
                --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
                --query "StandardOutputContent" --output text
              exit 1
            fi
            sleep 15
          done
          echo "Timed out waiting for sweep to complete"
          exit 1

      - name: Notify Telegram on success
        if: success()
        run: |
          # Read pre-formatted Telegram summaries from EC2 (written by Python sweep script)
          TG_CMD=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --instance-ids "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --parameters commands='["cat /home/ec2-user/trading-humming-bot/backtest/results/*_telegram.txt 2>/dev/null"]' \
            --timeout-seconds 30 \
            --query "Command.CommandId" --output text)

          for i in $(seq 1 6); do
            STATUS=$(aws ssm get-command-invocation --command-id "$TG_CMD" \
              --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
              --query "Status" --output text 2>/dev/null || echo "Pending")
            [ "$STATUS" = "Success" ] && break
            [ "$STATUS" = "Failed" ] && break
            sleep 5
          done

          TG_SUMMARY=$(aws ssm get-command-invocation --command-id "$TG_CMD" \
            --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
            --query "StandardOutputContent" --output text 2>/dev/null || echo "Sweep complete (summary unavailable)")

          # URL-encode newlines for curl
          TG_ENCODED=$(echo "$TG_SUMMARY" | python3 -c "import sys,urllib.parse; print(urllib.parse.quote(sys.stdin.read().strip(), safe=''))")

          curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=HTML \
            --data-urlencode "text=$TG_SUMMARY"

      - name: Notify Telegram on failure
        if: failure()
        run: |
          curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=HTML \
            -d text="❌ <b>Weekly Sweep FAILED</b>%0A•••%0A🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

  # ── Job 3: Auto-apply recommendations ─────────────────────────
  auto-apply:
    name: Auto-Apply Params
    needs: run-sweep
    if: success()
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Read sweep results from EC2
        id: results
        run: |
          RESULTS_CMD=$(aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --instance-ids "${{ secrets.EC2_INSTANCE_ID }}" \
            --region "${{ env.AWS_REGION }}" \
            --parameters commands='["cat /home/ec2-user/trading-humming-bot/backtest/results/*_sweep.json 2>/dev/null"]' \
            --timeout-seconds 30 \
            --query "Command.CommandId" --output text)

          for i in $(seq 1 6); do
            STATUS=$(aws ssm get-command-invocation --command-id "$RESULTS_CMD" \
              --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
              --query "Status" --output text 2>/dev/null || echo "Pending")
            [ "$STATUS" = "Success" ] && break
            [ "$STATUS" = "Failed" ] && break
            sleep 5
          done

          aws ssm get-command-invocation --command-id "$RESULTS_CMD" \
            --instance-id "${{ secrets.EC2_INSTANCE_ID }}" \
            --query "StandardOutputContent" --output text 2>/dev/null > /tmp/sweep_results.json

          if [ ! -s /tmp/sweep_results.json ]; then
            echo "No sweep results found"
            echo "has_changes=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Sweep results retrieved ($(wc -c < /tmp/sweep_results.json) bytes)"

      - name: Apply recommendations
        id: apply
        run: |
          pip install -q pyyaml 2>/dev/null || true
          python3 backtest/apply_sweep.py config/strategy.yaml < /tmp/sweep_results.json

      - name: Commit and push if changed
        id: commit
        run: |
          git diff --quiet config/strategy.yaml && {
            echo "No changes to apply"
            echo "has_changes=false" >> $GITHUB_OUTPUT
            exit 0
          }

          echo "has_changes=true" >> $GITHUB_OUTPUT
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add config/strategy.yaml
          git commit -m "auto: apply sweep-recommended params $(date -u +'%Y-%m-%d')

          Sweep-based param optimization applied automatically.
          Bot will redeploy with new config.

          Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
          git push origin main 2>&1
          echo "Pushed config changes — deploy-rust.yml will trigger"

      - name: Notify Telegram — params applied
        if: steps.commit.outputs.has_changes == 'true'
        run: |
          curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=HTML \
            -d text="🔧 <b>Sweep Params Auto-Applied</b>%0A•••%0A📝 config/strategy.yaml updated%0A🚀 Bot redeploying with new params...%0A⏰ $(date -u +'%Y-%m-%d %H:%M UTC')"

      - name: Notify Telegram — no changes needed
        if: steps.commit.outputs.has_changes != 'true'
        run: |
          curl -s -X POST \
            "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=HTML \
            -d text="✅ <b>Sweep Complete</b> — live params already optimal, no changes needed"
